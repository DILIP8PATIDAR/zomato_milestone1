[build]
builder = "DOCKERFILE"
watchPatterns = ["zomato_recommendation/**"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
